spring:
  application:
    name: soa-eureka-server
server:
  port: 8761

eureka:
  instance:
    hostname: soa-eureka-server
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka