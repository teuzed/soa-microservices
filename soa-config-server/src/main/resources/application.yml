server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    name: soa-config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/soa-configurations