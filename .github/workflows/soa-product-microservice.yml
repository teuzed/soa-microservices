name: Build and Push Docker Image - PRODUCT MICROSERVICE

on:
  push:
    branches:
      - develop
    paths:
      - 'soa-product-microservice/**'
  pull_request:
    branches:
      - develop
    paths:
      - 'soa-product-microservice/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}

      - name: Build Docker Image
        run: |
          cd soa-user-microservice
          docker build -t teuzed/soa-product-microservice:latest -t teuzed/soa-product-microservice:${{ github.sha }} .

      - name: Push Docker Images
        run: |
          docker push teuzed/soa-product-microservice:latest
          docker push teuzed/soa-product-microservice:${{ github.sha }}
